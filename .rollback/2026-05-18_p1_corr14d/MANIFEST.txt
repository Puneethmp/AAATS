ROLLBACK BASELINE — 2026-05-18 P1 corr14d silent-zero fix
==========================================================

Scope:
  Two-file change addressing C1 stat_arb's silently-zero 14d correlation:
    - trading/live_paper_runner.py — N_BARS_HOURLY 120 → 400 so the 336-bar
      14d correlation window actually has enough data
    - trading/stat_arb.py — _rolling_correlation returns None on insufficient
      data; caller skips cache write and logs warning instead of poisoning
      stat_arb_health.json with a bogus 0.0
  Plus one-time deploy step: wipe poisoned data/stat_arb_health.json so the
  new code computes a fresh value on first cycle rather than waiting 4 days
  for the 7-day staleness gate to expire.

Pre-flight grep (callers of _rolling_correlation):
  /app/trading/stat_arb.py:128  (definition)
  /app/trading/stat_arb.py:310  (single call site)
  No other callers — safe to change return type from float to float | None.

Pre-deploy state:
  Branch HEAD SHA:                f137cb3  (fix(strategies): C3 evaluate exits for held positions outside per-cycle picks)
  Container image ID:             sha256:140b0ae1645f0db97a087f2a54efb0d6eb12b23fa83ada14a575c5b81eca0637
  trading/live_paper_runner.py:   15929fb5e31e9c55d663366b9da9cbac751f90c3cc8f10fb9840ba5650966029
  trading/stat_arb.py:            40242ebe46434911b22cc0a33afd6215ab3ca00eef43fb0c0e895f6489143b2c
  data/stat_arb_health.json:      62aaf2481b6e9344d2176f44803bd8c416aa77877be01d95cb869efafd470ebc
                                  (content: {"BTC/USDT_ETH/USDT": {"eg_pvalue": 0.6867, "corr_14d": 0.0,
                                   "checked_at": "2026-05-15T08:09:25.790249+00:00", "pair_healthy": false}})

Expected post-deploy delta:
  fetch_crypto_hourly returns ~400 bars (was ~120)
  stat_arb_health.json regenerated by first post-deploy cycle with corr_14d
    in the expected 0.6-0.95 range for BTC/ETH 14d, pair_healthy likely True
  If pair_healthy=True AND |z| > 1.8 at evaluation moment, C1 fires a paired
    BTC BUY + ETH SELL (or reverse depending on z sign). z was +4.74 in
    stat_arb_state pre-session.
  If C1 fires BUY: market_peaks.crypto in risk_engine_state.json should either
    advance (equity up) or hold (~unchanged after fees). Closes P0.C1
    empirical proof gap from [[aaats-p0-reconcile-2026-05-18]].

Rollback procedure if any verification fails:
  1. SCP live_paper_runner.py.pre and stat_arb.py.pre back as .tmp on box, mv -f atomic swap each
  2. Restore data/stat_arb_health.json from stat_arb_health.json.pre via docker exec sh -c 'cat > /app/data/stat_arb_health.json'
  3. docker compose -f deployment/docker-compose.yml up -d --build --no-deps aaats-paper-crypto
  4. Confirm SHAs match 15929fb5/40242ebe; cache file restored
  5. Investigate before re-attempting
