# Phase κ1 storage layer dependencies.
# Pin major versions only; minor/patch float (parity-freeze applies after κ5).
asyncpg>=0.30,<0.32
redis>=5.0,<6
alembic>=1.13,<2
psycopg2-binary>=2.9,<3   # alembic only; runtime uses asyncpg
